<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="Xamarin_Layouts.MenuPage"
             Title="Menu">
  <ContentPage.Content>
    <StackLayout BackgroundColor="#F2F2F2" Padding="12">
      <Button Text="Home" TextColor="White" BackgroundColor="Gray" Command="{Binding GoHomeCommand}" />
      <Button Text="Login" TextColor="White" BackgroundColor="Gray" Command="{Binding GoLoginCommand}" />
      <Button Text="Button Grid" TextColor="White" BackgroundColor="Gray" Command="{Binding GoButtonGridCommand}" />
      <Button Text="Grid Layout" TextColor="White" BackgroundColor="Gray" Command="{Binding GoGridLayoutCommand}" />
      <Button Text="Survey" TextColor="White" BackgroundColor="Gray" Command="{Binding GoSurveyLayoutCommand}" />
	    <Button Text="Settings" TextColor="White" BackgroundColor="Gray" Command="{Binding GoSettingsCommand}" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>
